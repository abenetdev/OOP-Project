public class PaymentProcessor {
    public void processPayment(Customer customer, double amount) {
        System.out.println("Processing payment of $" + amount + " for " + customer.getName());
        // Integration with payment API would go here
    }
}
